#include "main.h"
/**
 * add - addition of two numbers
 *
 * @a: numbers to  be added
 * @b: value to be added to a
 * Return: result
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
