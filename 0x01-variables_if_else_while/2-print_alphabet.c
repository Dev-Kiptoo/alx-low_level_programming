#include <stdio.h>
/**
 * Main - Entry point
 *
 * Return : Always 0 (Success)
 */
int main(void)
	/* returns my value*/
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	putchar(a);
	putchar('\n');
	return (0);
}

